query fetchExtraActivity($id: ID!) {
    extraActivity(id: $id, idType: SLUG) {
        slug
        title
        featuredImage {
            node {
                sourceUrl
            }
        },
        content
        excerpt
    }
}
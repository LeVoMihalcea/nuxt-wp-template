query fetchPost($id: ID!) {
    post(id: $id, idType: ID) {
        id
        title
        content
        date
        featuredImage {
            node {
                mediaItemUrl
            }
        }
    }
}